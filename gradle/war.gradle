apply plugin: "war"

bootWar {
    mainClassName = "com.weville.beadude.BeaHipsterApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
